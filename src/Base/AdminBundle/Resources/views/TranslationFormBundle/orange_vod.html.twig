{% trans_default_domain 'BaseAdminBundle' %}
{{ form_errors(form) }}
<div class="a2lix_translations tabbable">
    {% set currentLocale = (app.request.get('locale')) ? app.request.get('locale') : form.vars.default_locale %}
    {% set headerTranslations = (form.parent.children.header.vars.data is not null) ? form.parent.children.header.vars.data.translations : null %}
    {% set videoTranslations = (form.parent.children.video.vars.data is not null) ? form.parent.children.video.vars.data.translations : null %}
    {% include 'BaseAdminBundle:Block:languages.tabs.html.twig' %}

    <div class="a2lix_translationsFields tab-content">
        {% for translationsFields in form %}
            {% set locale = translationsFields.vars.name %}
            {% if is_granted('ROLE_TRANSLATOR_FR') %}
                {% set currentLocale = 'fr' %}
            {% elseif is_granted('ROLE_TRANSLATOR_EN') %}
                {% set currentLocale = 'en' %}
            {% elseif is_granted('ROLE_TRANSLATOR_ES') %}
                {% set currentLocale = 'es' %}
            {% elseif is_granted('ROLE_TRANSLATOR_ZH') %}
                {% set currentLocale = 'zh' %}
            {% endif %}
            <div class="{% if app.request.get('code') is not null %}popin-a2lix-{{ locale }}{% else %}main-a2lix-{{ locale }}{% endif %} a2lix_translationsFields-{{ locale }} tab-pane {% if currentLocale == locale %}active{% endif %}">
                <div class="row">
                    <div class="col-md-8">
                        <div class="box box-success">
                            <div class="box-header">
                                <h4 class="box-title">
                                    {{ 'group.main_infos'|trans }}
                                </h4>
                            </div>
                            {% if form.vars.default_locale == locale %}
                                <div class="box-body">
                                    <div class="sonata-ba-collapsed-fields">
                                        {{ form_row(form.parent.children.header) }}
                                        {{ form_row(form.parent.children.video) }}
                                        {{ form_row(translationsFields.title) }}
                                        {{ form_row(translationsFields.introduction) }}
                                    </div>
                                </div>
                            {% else %}
                                <div class="box-body">
                                    <div class="sonata-ba-collapsed-fields">
                                        <div class="form-group">
                                            <header>
                                                Image cover
                                            </header>
                                            <div>
                                                <label>
                                                    Alt
                                                </label>
                                                <span>
                                                {% if headerTranslations is not null and
                                                    headerTranslations.get(locale) is not null %}
                                                    {{ headerTranslations.get(locale).alt }}
                                                {% endif %}
                                                </span>
                                            </div>
                                            {% if form.parent.children.header.vars.value is not null %}
                                            <a target="_blank" href="{{ path('admin_base_core_mediaimagesimple_edit', {'id': form.parent.children.header.vars.value}) }}">modifier les traductions</a>
                                            {% endif %}
                                        </div>
                                        <div class="form-group">
                                            <header>
                                                Vidéo
                                            </header>
                                            <div>
                                                <label>
                                                    Titre
                                                </label>
                                                <span>
                                                {% if videoTranslations is not null and
                                                videoTranslations.get(locale) is not null %}
                                                    {{ videoTranslations.get(locale).title }}
                                                {% endif %}
                                                </span>
                                            </div>
                                            {% if form.parent.children.video.vars.value is not null %}
                                                <a target="_blank" href="{{ path('admin_base_core_mediavideo_edit', {'id': form.parent.children.video.vars.value}) }}">modifier les traductions</a>
                                            {% endif %}
                                        </div>
                                        {{ form_row(translationsFields.title) }}
                                        {{ form_row(translationsFields.introduction) }}
                                    </div>
                                </div>
                            {% endif %}
                        </div>
                    </div>
                    <div class="col-md-4">
                        {% if form.vars.default_locale == locale %}
                            <div class="box box-success">
                                <div class="box-header">
                                    <h4 class="box-title">
                                        {{ 'group.orange_series.publish_parameters'|trans }}
                                    </h4>
                                </div>
                                <div class="box-body">
                                    <div class="sonata-ba-collapsed-fields">

                                        {% if is_granted('ROLE_TRANSLATOR_FR') or is_granted('ROLE_TRANSLATOR_EN') or is_granted('ROLE_TRANSLATOR_ES') or is_granted('ROLE_TRANSLATOR_ZH') %}
                                            {{ form_row(translationsFields.status, {'attr': {'class': 'translator'}}) }}
                                        {% elseif is_granted('ROLE_JOURNALIST') %}
                                            {{ form_row(translationsFields.status, {'attr': {'class': 'journalist'}}) }}
                                        {% elseif is_granted('ROLE_TRANSLATOR_MASTER') %}
                                            {{ form_row(translationsFields.status, {'attr': {'class': 'master_translator'}}) }}
                                        {% elseif is_granted('ROLE_ORANGE') %}
                                            {{ form_row(translationsFields.status, {'attr': {'class': 'orange'}}) }}
                                        {% else %}
                                            {{ form_row(translationsFields.status) }}
                                        {% endif %}

                                        <div class="form-group fdc-input-border">
                                            {{ form_widget(form.parent.children.translate) }}
                                            {{ form_label(form.parent.children.translate) }}
                                            {{ form_widget(form.parent.children.translateOptions) }}
                                            {{ form_row(form.parent.children.priorityStatus) }}
                                        </div>
                                    </div>
                                    {% if app.request.get('id') is not null %}
                                        <button type="submit" class="btn btn-success" name="btn_update_and_"><i class="fa fa-save"></i> Mettre à jour</button>
                                        <button type="submit" class="btn btn-success" name="btn_update_and_list"><i class="fa fa-save"></i> <i class="fa fa-list"></i> Mettre à jour et fermer</button>
                                    {% else %}
                                        <button type="submit" class="btn btn-success" name="btn_create_and_edit"><i class="fa fa-save"></i> Créer</button>
                                        <button type="submit" class="btn btn-success" name="btn_create_and_list"><i class="fa fa-save"></i> <i class="fa fa-list"></i> Créer et retourner à la liste</button>
                                    {% endif %}
                                </div>
                            </div>
                        {% else %}
                            <div class="box box-success">
                                <div class="box-header">
                                    <h4 class="box-title">
                                        {{ 'group.french_version'|trans }}
                                    </h4>
                                </div>
                                <div class="box-body">
                                    <div class="sonata-ba-collapsed-fields">
                                        <div class="form-group">
                                            <label class="control-label">
                                                {{ 'form.label_title'|trans }}
                                            </label>
                                            {% if form.children.fr.vars.value is not null %}
                                                <div>
                                                    {{ form.children.fr.vars.value.title }}
                                                </div>
                                            {% endif %}
                                        </div>
                                        
                                        <div class="form-group">
                                            <label class="control-label">
                                                {{ 'form.label_introduction'|trans }}
                                            </label>
                                            {% if form.children.fr.vars.value is not null %}
                                                <div>
                                                    {{ form.children.fr.vars.value.introduction|raw|striptags|truncate(300)|nl2br }}
                                                </div>
                                            {% endif %}
                                        </div>
                                    </div>
                                </div>
                            </div>
                        {% endif %}
                    </div>
                </div>
           
                <div class="row">
                    {% if form.vars.default_locale == locale %}
                        <div class="col-md-12">
                            <div class="box box-success">
                                <div class="box-header">
                                    <h4 class="box-title">
                                        {{ 'group.orange_vod.widgets'|trans }}
                                    </h4>
                                </div>
                                <div class="box-body">
                                    <div class="fdc-widgets" data-form-widget="collection">
                                        {% for widget in form.parent.children.widgets.children %}
                                            <div class="item base-widget">
                                                <h4><strong>{{ 'form.widget.film_vod'|trans }}</strong></h4>
                                                {{ form_row(widget._type) }}
                                                {{ form_row(widget.position) }}
                                                {{ form_row(widget.section) }}
                                                {{ form_row(widget.image) }}
                                                {{ form_widget(attribute(widget.children.translations, locale)) }}
                                                {{ form_row(widget.copy) }}
                                                {{ form_row(widget.producer) }}
                                                <a class="btn btn-danger btn-sm btn-outline remove_item">
                                                    <i class="fa fa-minus-circle"></i> {{ 'action_delete'|trans({}, 'SonataAdminBundle') }}
                                                </a>
                                            </div>
                                        {% endfor %}
                                    </div>
                                    {{ 'form.orange_vod.helper_widgets'|trans }}<br/>
                                    <a class="btn btn-primary btn-add-base-widget" data-prototype="{{ form_widget(form.parent.children.widgets.vars.prototypes.orange_widget_film_vod_type) | escape }}"><i class="icon-plus"></i>{{ 'form.widget.film_vod'|trans }}</a>
                                    </div>
                            </div>
                        </div>
                    {% else %}
                        <div class="col-md-8">
                            <div class="box box-success">
                                <div class="box-header">
                                    <h4 class="box-title">
                                        {{ 'group.orange_studio.widgets'|trans }}
                                    </h4>
                                </div>

                                <div class="box-body">
                                    <div class="fdc-widgets" data-form-widget="collection">
                                        {% for widget in form.parent.children.widgets.children %}
                                            <div class="item base-widget">
                                                <h4><strong>{{ 'form.widget.film_vod'|trans }}</strong></h4>
                                                {{ form_row(widget._type) }}
                                                {{ form_row(widget.position) }}
                                                {% set section_label = '' %}
                                                {% for choice in widget.children.section.vars.choices %}
                                                    {% if choice.value == widget.children.section.vars.value %}
                                                        {% set section_label = choice.label %}
                                                    {% endif %}
                                                {% endfor %}
                                                <div class="form-group">
                                                    <label class="control-label">
                                                        {{ 'form.label_orange_widget_film_vod_section'|trans }}
                                                    </label>
                                                    <div>
                                                        <div class="form-group">
                                                            <div>
                                                                {{ section_label }}
                                                            </div>
                                                        </div>
                                                    </div>
                                                </div>
                                                {% set imageTranslations = (widget.children.image.vars.data is not null) ? widget.children.image.vars.data.translations : null %}
                                                <div class="form-group">
                                                    <label class="control-label">
                                                        {{ 'form.label_image'|trans }}
                                                    </label>
                                                    <div>
                                                        <div class="form-group">
                                                            <div>
                                                                <b>{{ 'form.label_alt_img'|trans }}:</b>
                                                                {% if imageTranslations is not null and imageTranslations.get(locale) is not null %}
                                                                    {{ imageTranslations.get(locale).alt }}
                                                                {% endif %}
                                                            </div>
                                                        </div>
                                                        {% if widget.children.image.vars.value is not null %}
                                                            <a target="_blank" href="{{ path('admin_base_core_mediaimagesimple_edit', {'id': widget.children.image.vars.value}) }}">
                                                                modifier les traductions
                                                            </a>
                                                        {% endif %}
                                                    </div>
                                                </div>
                                                
                                                {{ form_widget(attribute(widget.children.translations, locale)) }}
                                                {{ form_row(widget.copy) }}
                                                {{ form_row(widget.producer) }}
                                            </div>
                                        {% endfor %}
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="box box-success">
                                <div class="box-header">
                                    <h4 class="box-title">
                                        {{ 'group.french_version'|trans }}
                                    </h4>
                                </div>
                                <div class="box-body">
                                    <div class="sonata-ba-collapsed-fields">
                                        {% for widget in form.parent.children.widgets.children %}
                                            <div class="item base-widget">
                                                
                                                    <h4><strong>{{ 'form.widget.film_vod'|trans }}</strong></h4>
                                                    <div class="form-group">
                                                        {% set section_label = '' %}
                                                        {% for choice in widget.children.section.vars.choices %}
                                                            {% if choice.value == widget.children.section.vars.value %}
                                                                {% set section_label = choice.label %}
                                                            {% endif %}
                                                        {% endfor %}
                                                        <label>
                                                            {{ 'form.label_orange_widget_film_vod_section'|trans }}
                                                        </label>
                                                        <div>
                                                            {{ section_label }}
                                                        </div>
                                                    </div>
                                                    <div class="form-group">
                                                        {% set imageTranslations = (widget.vars.data.image is not null) ? widget.vars.data.image.translations : null %}
                                                        <label>
                                                            Informations image
                                                        </label>
                                                        <div>
                                                            <b>{{ 'form.label_alt_img'|trans }}:</b>
                                                            {% if imageTranslations is not null and imageTranslations.get('fr') is not null %}
                                                                {{ imageTranslations.get('fr').alt }}
                                                            {% endif %}
                                                        </div>
                                                    </div>
                                                    <div class="form-group">
                                                        <label>
                                                            {{ 'form.label_orange_widget_film_vod_title'|trans }}
                                                        </label>
                                                        <div>
                                                            {% if widget.children.translations.fr.vars.value is not null %}
                                                                {{ widget.children.translations.fr.vars.value.title }}
                                                            {% endif %}
                                                        </div>
                                                    </div>
                                                    <div class="form-group">
                                                        <label>
                                                            {{ 'form.label_orange_widget_film_vod_description'|trans }}
                                                        </label>
                                                        <div>
                                                            {% if widget.children.translations.fr.vars.value is not null %}
                                                                {{ widget.children.translations.fr.vars.value.description }}
                                                            {% endif %}
                                                        </div>
                                                    </div>
                                                    <div class="form-group">
                                                        <label>
                                                            {{ 'form.label_orange_widget_film_vod_copy'|trans }}
                                                        </label>
                                                        <div>
                                                            {% if widget.children.translations.fr.vars.value is not null %}
                                                                {{ widget.children.copy.vars.value }}
                                                            {% endif %}
                                                        </div>
                                                    </div>
                                                    <div class="form-group">
                                                        <label>
                                                            {{ 'form.label_orange_widget_film_vod_producer'|trans }}
                                                        </label>
                                                        <div>
                                                            {% if widget.children.translations.fr.vars.value is not null %}
                                                                {{ widget.children.producer.vars.value }}
                                                            {% endif %}
                                                        </div>
                                                    </div>
                                            </div>
                                        {% endfor %}
                                    </div>
                                </div>
                            </div>
                        </div>
                    {% endif %}
                </div>
   
                {% if form.vars.default_locale != locale %}
                <div class="row">
                    <div class="col-md-4">
                        {% if is_granted('ROLE_TRANSLATOR_FR') or is_granted('ROLE_TRANSLATOR_EN') or is_granted('ROLE_TRANSLATOR_ES') or is_granted('ROLE_TRANSLATOR_ZH') %}
                            {{ form_row(translationsFields.status, {'attr': {'class': 'translator'}}) }}
                        {% elseif is_granted('ROLE_JOURNALIST') %}
                            {{ form_row(translationsFields.status, {'attr': {'class': 'journalist'}}) }}
                        {% elseif is_granted('ROLE_TRANSLATOR_MASTER') %}
                            {{ form_row(translationsFields.status, {'attr': {'class': 'master_translator'}}) }}
                        {% elseif is_granted('ROLE_ORANGE') %}
                            {{ form_row(translationsFields.status, {'attr': {'class': 'orange'}}) }}
                        {% else %}
                            {{ form_row(translationsFields.status) }}
                        {% endif %}
                    </div>
                </div>
                {% endif %}
            </div>
        {% endfor %}
    </div>
</div>