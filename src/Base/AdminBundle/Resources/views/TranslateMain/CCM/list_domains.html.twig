{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}
{% trans_default_domain 'BaseAdminBundle' %}

{% block field %}
    {% if admin.hasRoute('edit') %}
        {#{{ dump() }}#}
            {% if object.translations[app.request.getLocale()] is defined and object.translations[app.request.getLocale()] is not null %}
                {% set translation = object.translations[app.request.getLocale()] %}
                {% if field_description is not null and field_description.options['choices'] is not null %}
                    {% if field_description.options['choices'][translation.domain] is defined %}
                        {{ field_description.options['choices'][translation.domain] }}
                    {% endif %}
                {% endif %}
            {% endif %}
    {% endif %}
{% endblock %}