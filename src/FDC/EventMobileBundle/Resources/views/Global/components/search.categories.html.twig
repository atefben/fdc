{% trans_default_domain 'FDCEventBundle' %}

<div id="horizontal-menu" class="owl-carousel sliderDrag videosSlider">
    <div class="vid">
        <p><a href="#" class="active all">{{ 'search.result.category.tout'|trans }} (<span>{{ nbResult }}</span>)</a></p>
    </div>
    <div class="vid">
        <p><a href="#" data-ajax="{{ path('fdc_eventmobile_search_searchajax',{'searchFilter':'artist', 'searchTerm': searchTerm}) }}" class="artists ajax">{{ 'search.result.category.artistes'|trans }} (<span>{{ searchResult.count.artist }}</span>)</a></p>
    </div>
    <div class="vid">
        <p><a href="#" data-ajax="{{ path('fdc_eventmobile_search_searchajax',{'searchFilter':'film', 'searchTerm': searchTerm}) }}" class="films ajax">{{ 'search.result.category.films'|trans }} (<span>{{ searchResult.count.film }}</span>)</a></p>
    </div>
    <div class="vid">
        <p><a href="#" data-ajax="{{ path('fdc_eventmobile_search_searchajax',{'searchFilter':'news', 'searchTerm': searchTerm}) }}" class="news ajax">{{ 'search.result.category.actualites'|trans }} (<span>{{ searchResult.count.actualite }}</span>)</a></p>
    </div>
    <div class="vid">
        <p><a href="#" data-ajax="{{ path('fdc_eventmobile_search_searchajax',{'searchFilter':'info', 'searchTerm': searchTerm}) }}" class="infos ajax">{{ 'search.result.category.infos'|trans }} (<span>{{ searchResult.count.info }}</span>)</a></p>
    </div>
    <div class="vid">
        <p><a href="#" data-ajax="{{ path('fdc_eventmobile_search_searchajax',{'searchFilter':'statement', 'searchTerm': searchTerm}) }}" class="communiques ajax">{{ 'search.result.category.communiques'|trans }} (<span>{{ searchResult.count.statement }}</span>)</a></p>
    </div>
    <div class="vid">
        <p><a href="#" data-ajax="{{ path('fdc_eventmobile_search_searchajax',{'searchFilter':'media', 'searchTerm': searchTerm}) }}" class="medias ajax">{{ 'search.result.category.medias'|trans }} (<span>{{ searchResult.count.media }}</span>)</a></p>
    </div>
    <div class="vid">
        <p><a href="#" data-ajax="{{ path('fdc_eventmobile_search_searchajax',{'searchFilter':'event', 'searchTerm': searchTerm}) }}" class="events ajax">{{ 'search.result.category.evenements'|trans }} (<span>{{ searchResult.count.event }}</span>)</a></p>
    </div>
    <div class="vid">
        <p><a href="#" data-ajax="{{ path('fdc_eventmobile_search_searchajax',{'searchFilter':'participate', 'searchTerm': searchTerm}) }}" class="participate ajax">{{ 'search.result.category.participer'|trans }} (<span>{{ searchResult.count.participate }}</span>)</a></p>
    </div>
</div>