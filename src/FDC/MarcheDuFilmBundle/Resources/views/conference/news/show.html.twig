{% extends 'FDCMarcheDuFilmBundle::base.html.twig' %}

{% block main %}
    <div id="main" class="home loading">
        {% include 'FDCMarcheDuFilmBundle:shared:conferenceSubHeader.html.twig' %}
        <div class="bigContainer">
            <div class="our-news newsPage mdf">
                <h3>{{ newsPageContent.title }}</h3>
                <div class="innerWrapper">
                    {% include 'FDCMarcheDuFilmBundle::conference/news/partials/newsBlock.html.twig' with { 'news': news } %}
                </div>
            </div>

            {% if news|length >= 9 %}
                <a href="#" id="load-more-conference-articles" data-slug="{{ route_params['slug'] }}">
                    <div class="moreArticles">
                        <span>{{ 'news.block.load_more_button'|trans }}</span>
                    </div>
                </a>
            {% endif %}
        </div>
    </div>
    {% include 'FDCMarcheDuFilmBundle:shared:floatingButtons.html.twig' %}
    {% include 'FDCMarcheDuFilmBundle::partials/contactBlock.html.twig' with { 'contact': contact } %}
{% endblock main %}
