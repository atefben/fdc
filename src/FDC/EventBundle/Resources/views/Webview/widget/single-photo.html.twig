{% if widget.header is defined %}
	{% set img = widget.header|trans_fallback('file') %}
{% elseif widget.gallery is defined %}
	{% set firstMediaGallery = widget.gallery.media|first %}
	{% set img = widget.gallery.media|first %}
		<p>widget.gallery.medias|first</p>
		{{ dump(widget.gallery.medias|first) }}
		{{ firstMediaGallery }}
	}
{% endif %}