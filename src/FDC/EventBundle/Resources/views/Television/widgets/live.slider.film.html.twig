{% trans_default_domain 'FDCEventBundle' %}

{% if film.associatedMediaVideos|last %}
{% set video = film.associatedMediaVideos|last.mediaVideo %}
<div class="vid shadow-bottom">
    <div class="image-wrapper">
        <img src="{% path video.image|trans_fallback('file'), '975x616' %}"
             srcset="{% path video.image|trans_fallback('file'), '573x362' %} 1x, {% path video.image|trans_fallback('file'), '1280x808' %} 2x"
             alt="">
    </div>
    <a class="linkVid" href="#"></a>
    <div class="info">
        <div class="picto"><i class="icon icon_video"></i>
        </div>
        <div class="info-container">
            <div class="vCenter">
                <div class="vCenterKid">
                    <a href="" class="category">{{ video.theme }}</a><span
                            class="date">{{ video.createdAt|date('d.m.Y') }}</span> . <span
                            class="hour">{{ video.createdAt|date('d.m.Y') }}</span>
                    <a href="#" class="titleLink">{{ video|trans_fallback('title') }}</a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endif %}
