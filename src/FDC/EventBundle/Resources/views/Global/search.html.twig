<form action="{{ path('fdc_event_global_search') }}" method="get" {{ form_enctype(searchForm) }}>
    {{ form_widget(searchForm.search, { 'id': 'inputSearch', 'class': 'suggestSearch' }) }}
    <input id="submitSearch" type="submit" value="" />
    {{ form_rest(searchForm) }}
</form>
