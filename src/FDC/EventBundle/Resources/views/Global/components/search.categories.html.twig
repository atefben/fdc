{% trans_default_domain 'FDCEventBundle' %}

<div id="colSearch">
    <ul>
        <li>
            <a href="#" class="active all">
                {{ 'search.result.category.tout'|trans }}
                <span>{{ nbResult }}</span>
            </a>
        </li>
        <li>
            <a href="#" data-ajax="ajax/artist/{{ searchTerm }}" class="artists ajax">
                {{ 'search.result.category.artistes'|trans }}
                <span>{{ searchResult.count.artist }}</span>
            </a>
        </li>
        <li>
            <a href="#" data-ajax="ajax/film/{{ searchTerm }}" class="films ajax">
                {{ 'search.result.category.films'|trans }}
                <span>{{ searchResult.count.film }}</span>
            </a>
        </li>
        <li>
            <a href="#" data-ajax="ajax/news/{{ searchTerm }}" class="news ajax">
                {{ 'search.result.category.actualites'|trans }}
                <span>{{ searchResult.count.actualite }}</span>
            </a>
        </li>
        <li>
            <a href="#" data-ajax="ajax/info/{{ searchTerm }}" class="infos ajax">
                {{ 'search.result.category.infos'|trans }}
                <span>{{ searchResult.count.info }}</span>
            </a>
        </li>
        <li>
            <a href="#" data-ajax="ajax/statement/{{ searchTerm }}" class="communiques ajax">
                {{ 'search.result.category.communiques'|trans }}
                <span>{{ searchResult.count.statement }}</span>
            </a>
        </li>
        <li>
            <a href="#" data-ajax="ajax/media/{{ searchTerm }}" class="medias ajax">
                {{ 'search.result.category.medias'|trans }}
                <span>{{ searchResult.count.media }}</span>
            </a>
        </li>
        <li>
            <a href="#" data-ajax="ajax/event/{{ searchTerm }}" class="events ajax">
                {{ 'search.result.category.evenements'|trans }}
                <span>{{ searchResult.count.event }}</span>
            </a>
        </li>
        <li>
            <a href="#" data-ajax="ajax/participate/{{ searchTerm }}" class="participate ajax">
                {{ 'search.result.category.participer'|trans }}
                <span>{{ searchResult.count.participate }}</span>
            </a>
        </li>
    </ul>
</div>