<div class="push-container">
    <div class="container">

        <div class="push">
            <div class="image-wrapper">
                {% if homepage.pushsMainImage1 is not null %}
                    {% set trans = homepage.pushsMainImage1.findTranslationByLocale(app.request.locale) %}
                    {% if trans is null or trans.file is null %}
                        {% set trans = homepage.pushsMainImage1.findTranslationByLocale('fr') %}
                    {% endif %}
                    <img src="{% path trans.file , '325x205' %}" srcset="{% path trans.file , '325x205' %} 1x, {% path trans.file , '650x410' %} 2x" alt="{{ trans.alt }} ">
                {% endif %}
            </div>
            <div class="vCenter">
                <div class="vCenterKid">
                    <h3>{{ homepage|trans_fallback('primaryPushTitle1')|raw }}</h3>
                </div>
            </div>
            <a {{ homepage|trans_fallback('primaryPushUrl1')|external_link }} href="{{ homepage|trans_fallback('primaryPushUrl1') }}"></a>
        </div>

        <div class="push">
            <div class="image-wrapper">
                {% if homepage.pushsMainImage2 is not null %}
                    {% set trans = homepage.pushsMainImage2.findTranslationByLocale(app.request.locale) %}
                    {% if trans is null or trans.file is null %}
                        {% set trans = homepage.pushsMainImage2.findTranslationByLocale('fr') %}
                    {% endif %}
                    <img src="{% path trans.file , '325x205' %}" srcset="{% path trans.file , '325x205' %} 1x, {% path trans.file , '650x410' %} 2x" alt="{{ trans.alt }} ">
                {% endif %}
            </div>
            <div class="vCenter">
                <div class="vCenterKid">
                    <h3>{{ homepage|trans_fallback('primaryPushTitle2')|raw }}</h3>
                </div>
            </div>
            <a {{ homepage|trans_fallback('primaryPushUrl2')|external_link }} href="{{ homepage|trans_fallback('primaryPushUrl2') }}"></a>
        </div>

        <div class="push">
            <div class="image-wrapper">
                {% if homepage.pushsMainImage3 is not null %}
                    {% set trans = homepage.pushsMainImage3.findTranslationByLocale(app.request.locale) %}
                    {% if trans is null or trans.file is null %}
                        {% set trans = homepage.pushsMainImage3.findTranslationByLocale('fr') %}
                    {% endif %}
                    <img src="{% path trans.file , '325x205' %}" srcset="{% path trans.file , '325x205' %} 1x, {% path trans.file , '650x410' %} 2x" alt="{{ trans.alt }} ">
                {% endif %}
            </div>
            <div class="vCenter">
                <div class="vCenterKid">
                    <h3>{{ homepage|trans_fallback('primaryPushTitle3')|raw }}</h3>
                </div>
            </div>
            <a {{ homepage|trans_fallback('primaryPushUrl3')|external_link }} href="{{ homepage|trans_fallback('primaryPushUrl3') }}"></a>
        </div>

    </div>
</div>