{% trans_default_domain 'FDCEventBundle' %}
{% extends 'FDCEventBundle::layout.html.twig' %}

{% block body %}
    <div id="main" class="list-article films-list loading honneur-invit canne-classic selection-officielle">
        <div class="bandeau-list">
            <div class="bandeau-list-img bandeau-head vCenter" style="background-image:url('//html.festival-cannes-2016.com.ohwee.fr/img/films/selection-officielle/selection-bandeau.jpg')">
                <div class="vCenterKid">
                    <h2 class="title title-list-header">{{ 'selection.title.selectionofficielle'|trans }}</h2>
                </div>
            </div>
                {% include 'FDCEventBundle:Movie:components/filters.html.twig' %}
        </div>
        <div class="container container-list">
            <section class="categorie-items">
                <div class="title-list-cat">
                    <h2 class="titre-document">{{ cinemaContent.description }}</h2>
                </div>

                {% for film in cinemaContent.film %}
                    <article style="background-image:url({{ film.image.path }})">
                        <div class="bck-hover">
                            <div class="contain_item">
                                <img src="{{ film.image.path }}" alt="images films">
                                <div class="infos-item-list">
                                    <span class="date-pres-item">{{ film.projectionDate|localizeddate('long', 'none', app.request.locale)  }} </span>
                                    <span class="heure-pres-item"> {{ film.projectionDate|date('H:i')  }} </span>
                                    <h3 class="title-item">{{ film.title }}</h3>
                                    <span class="nom-item">{{ film.author.fullName }}</span>
                                    <span class="date-item">{{ film.releaseDate|date('Y') }} - {{ film.duration }}</span>
                                    <p class="description-item">
                                        {{ film.description }}
                                    </p>
                                </div>
                            </div>
                        </div>
                    </article>
                {% endfor %}

            </section>
        </div>
        <div class="bandeau-list bandeau-list-footer push-footer vCenter">
            <div class="bandeau-list-img vCenterKid" style="background-image:url('//html.festival-cannes-2016.com.ohwee.fr/img/films/cover-bandeau-push.jpg');">
                <h2 class="title title-list-header">{{ 'selection.cinemaplage.title.competition'|trans }}</h2>
                <a href="#" class="bandeau-lien">
                    {% image '@FDCEventBundle/Resources/public/img/svg/arrow-right-gold.svg' %}
                    <img class="svg-arrow" src="{{ asset_url }}" alt="{{ 'selection.cinemaplage.btn.decouvrirlarubrique'|trans }}" />
                    {% endimage %}
                    {{ 'selection.cinemaplage.btn.decouvrirlarubrique'|trans }}
                </a>
            </div>
        </div>
    </div>

{% endblock body %}