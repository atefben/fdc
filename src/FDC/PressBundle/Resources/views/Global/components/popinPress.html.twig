{% trans_default_domain 'FDCPressBundle' %}

<div id="popin-press" class='popin'>
    <div class="container-popin">
        <strong>{{ 'press.popin.title.contenuverouille'|trans }}</strong>
        <p>{{ 'press.popin.text.cecontenuestexclusivementreservealapresse'|trans }}</p>
        {#<form action="{{ path('fdc_press_news_home') }}" method="post" {{ form_enctype(lockedForm) }}>#}
            {#{{ form_widget(lockedForm.password) }}#}
            {#<input type="submit" value="{{ 'press.header.btn.valider'|trans }}" />#}
            {#{{ form_rest(lockedForm) }}#}
        {#</form>#}
    </div>
</div>